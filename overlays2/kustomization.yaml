apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../base2

namespace: default

patches:
- target:
    kind: Deployment
    name: nginx-deployment-2
  patch: |-
    apiVersion: apps/v1
    kind: Deployment
    metadata:
      name: nginx-deployment-2
    spec:
      replicas: 2
